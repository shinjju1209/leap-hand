# 🦾 LEAP Hand 전시회 및 부스 운영 매뉴얼 (Booth Operator Manual)

본 매뉴얼은 **전시회, 학회, 박람회, 체험 부스** 등에서 LEAP Hand v1 (16 DOF) 로봇 핸드와 종합 키오스크 애플리케이션(`booth_app.py`)을 운영하는 현장 담당자를 위한 실무 가이드입니다.

---

## 📌 목차

1. [🚀 부스 오픈 퀵 스타트 (Quick Start)](#1-부스-오픈-퀵-스타트-quick-start)
2. [🔌 하드웨어 연결 및 전원 세팅](#2-하드웨어-연결-및-전원-세팅)
3. [💻 리눅스 환경 및 포트 권한 설정](#3-리눅스-환경-및-포트-권한-설정)
4. [🩺 3단계 사전 하드웨어 점검 (필수)](#4-3단계-사전-하드웨어-점검-필수)
5. [🎪 부스 키오스크 앱 실행 (`booth_app.py`)](#5-부스-키오스크-앱-실행-booth_apppy)
6. [🖐️ 체험 모드별 상세 운영 & 관람객 안내 가이드](#6-체험-모드별-상세-운영--관람객-안내-가이드)
7. [🚨 비상 상황 조치 및 안전 수칙](#7-비상-상황-조치-및-안전-수칙)
8. [🛠️ 현장 트러블슈팅 (FAQ)](#8-현장-트러블슈팅-faq)

---

## 1. 🚀 부스 오픈 퀵 스타트 (Quick Start)

부스 운영 시작 전 **딱 3분** 만에 점검하고 실행하는 표준 순서입니다:

```bash
# 1. 터미널 실행 및 작업 디렉토리 이동
cd ~/Documents/antigravity/charming-pascal/leap-hand
source .venv/bin/activate

# 2. 시리얼 포트 쓰기 권한 및 저지연 타이머 설정
sudo chmod 666 /dev/ttyUSB0
echo 1 | sudo tee /sys/bus/usb-serial/devices/ttyUSB0/latency_timer

# 3. 하드웨어 16개 모터 진단
python leap_hand_hardware_check.py --port /dev/ttyUSB0

# 4. 종합 부스 키오스크 앱 실행 (실물 하드웨어 모드)
python booth_app.py --mode hardware --port /dev/ttyUSB0
```

---

## 2. 🔌 하드웨어 연결 및 전원 세팅

### 1) 부품 구성 및 배선
* **LEAP Hand v1 (16개 모터)**: 거치대나 삼각대에 흔들림 없이 단단히 고정합니다.
* **5V 전원 어댑터 / SMPS**: LEAP Hand 메인 전원 단자에 연결합니다. (안정적인 5V 4A 이상 권장)
* **USB-RS485/UART 시리얼 어댑터**: PC USB 포트에 직접 연결합니다. (가급적 무전원 USB 허브 사용 지양)
* **웹캠 (Webcam)**: 관람객의 손이 정면에서 잘 보이도록 0.5m ~ 1m 높이에 설치합니다.

### 2) 안전 체크
* 로봇 손가락 주변에 케이블이나 이물질이 걸려 있지 않은지 확인합니다.
* 비상 시 손으로 전원 플러그를 바로 뽑을 수 있도록 전원 스위치 위치를 확인합니다.

---

## 3. 💻 리눅스 환경 및 포트 권한 설정

### 1) 시리얼 포트 확인
PC에 USB 변환기를 꽂은 후 포트명을 확인합니다.

```bash
ls /dev/ttyUSB*
# 출력 예: /dev/ttyUSB0
```

> 💡 포트 번호가 `/dev/ttyUSB1` 등으로 변경된 경우, 모든 명령어의 `--port` 뒤에 해당 포트명을 입력하면 됩니다.

### 2) 포트 권한 및 초고속 저지연(1ms) 모드 설정
리눅스 기본 USB 시리얼 지연(16ms)을 1ms로 단축하여 실시간 1:1 추종 반응 속도를 극대화합니다.

```bash
sudo chmod 666 /dev/ttyUSB0
echo 1 | sudo tee /sys/bus/usb-serial/devices/ttyUSB0/latency_timer
```

---

## 4. 🩺 3단계 사전 하드웨어 점검 (필수)

부스 오픈 전 로봇의 16개 모터 통신, 전압, 온도를 단계별로 점검합니다.

### [Step 1] 통신 및 센서 기본 점검 (토크 OFF)
모터에 힘을 주지 않고 센서값과 에러 유무만 빠르게 스캔합니다.

```bash
python leap_hand_hardware_check.py --port /dev/ttyUSB0
```

* **정상 확인 항목**:
  * `Motor IDs/model numbers`: `{0: 1240, 1: 1240, ..., 15: 1240}` (16개 모터 전부 표시)
  * `Input voltages`: 모든 모터 `5.0V ~ 5.2V` 범위
  * `Hardware errors`: `[0, 0, 0, ..., 0]` (모두 0)

### [Step 2] 안전 토크 유지 점검
로봇 손이 현재 자세를 유지하며 모터에 정상적으로 토크가 걸리는지 2초간 테스트합니다.

```bash
python leap_hand_hardware_check.py --port /dev/ttyUSB0 --torque-test --hold-seconds 2
```

### [Step 3] 손가락별 저속 순차 구동 점검
엄지, 검지, 중지, 약지가 순서대로 부드럽게 쥐어졌다 펴지는지 확인합니다.

```bash
python leap_hand_hardware_finger_test.py --port /dev/ttyUSB0
```

---

## 5. 🎪 부스 키오스크 앱 실행 (`booth_app.py`)

### 1) 기본 실행 (실물 로봇 연동 모드 - 추천 ⭐)
실물 로봇을 직접 제어하며, 쇼케이스 화면에서는 내장 3D 시뮬레이터가 함께 연동됩니다.

```bash
python booth_app.py --mode hardware --port /dev/ttyUSB0
```

### 2) 기타 실행 옵션

```bash
# [옵션 A] 시뮬레이션 전용 모드 (하드웨어 없이 웹캠과 3D MuJoCo로 시연)
python booth_app.py --mode mujoco

# [옵션 B] 실물 하드웨어 + 별도 외부 3D 뷰어 창 동시 구동
python booth_app.py --mode both --port /dev/ttyUSB0
```

### 3) RL 큐브 회전 단독 시연

```bash
# 기본 official policy + 안정 파지 상태로 MuJoCo 시연
python rl_sim2real_deploy.py --mode mujoco
```

MuJoCo 창에서 `A`를 누르면 +Z축 회전을 시작하고, `R`은 손·큐브·policy 내부 상태를 모두 초기화합니다. 큐브가 떨어져도 같은 전체 초기화가 자동 실행됩니다. 기본 checkpoint는 +Z 방향으로 학습되었으므로 현장 시연에서는 `1` 방향을 사용합니다.

실물에서 시연하기 전에는 모터 점검을 통과하고 `calibration/hardware_motors.yaml`이 현재 장비에서 기록한 파일인지 확인합니다.

```bash
python rl_sim2real_deploy.py --mode hardware \
  --port /dev/ttyUSB0 \
  --motor-calibration-file calibration/hardware_motors.yaml
```

실행 직후에는 큐브를 넣지 말고 손 주변을 비워 둡니다. 정확한 policy grasp는 큐브 지지가 없으면 실물 `middle_mcp_flex`가 도달하지 못하므로, 프로그램은 이 관절만 약 `+8.3°`로 완화한 cube-loading pose로 4초 동안 이동한 뒤 `Cube-loading pose ready and holding`을 출력하며 대기합니다. 이 문구를 확인한 다음 큐브를 손바닥에 놓고 `A`를 누릅니다. 그러면 큐브가 손가락을 지지하는 상태에서 정확한 MuJoCo 초기 grasp로 이동·검증하고, 실측 관절값으로 policy/GRU를 초기화하여 회전을 시작합니다. 이동 방향이 이상하거나 큐브가 끼면 즉시 `Space`를 누르고 5V 전원을 차단합니다. calibration 파일이 없으면 실물 RL 모드는 안전상 실행되지 않습니다.

---

## 6. 🖐️ 체험 모드별 상세 운영 & 관람객 안내 가이드

앱이 실행되면 **메인 홈 화면**이 나타납니다. 화면의 카드를 마우스로 클릭하거나 키보드 단축키로 모드를 전환합니다.

```text
[ 홈 화면 단축키 ]
  1 : 🖐️ 텔레오퍼레이션 (1:1 실시간 모션 추종)
  2 : ✂️ 가위바위보 대결 (인공지능 RPS)
  3 : 🎭 제스처 쇼케이스 (다양한 손동작 & 댄스 애니메이션)
  Q / ESC : 부스 앱 종료
```

---

### 🖐️ Mode 1. 텔레오퍼레이션 (1:1 실시간 손 추종)

관람객의 손 움직임을 웹캠으로 읽어 실물 로봇 손이 1:1로 똑같이 움직이는 최고 인기 체험 모드입니다.

#### 🎯 관람객 체험 진행 순서
1. **관람객 유도**: 카메라 정면 약 0.7m 거리에 오른손을 비추도록 안내합니다.
2. **편 손 보정 (`C` 키)**: 관람객에게 손바닥을 활짝 펴게 한 뒤 **`C`** 키를 누릅니다.
3. **주먹 보정 (`F` 키)**: 관람객에게 주먹을 꽉 쥐게 한 뒤 **`F`** 키를 누릅니다.
4. **추종 시작 (`A` 키)**: **`A`** 키를 누르면 화면에 초록색 `ARMED (추종 중)` 표시가 뜨며 로봇이 즉시 손가락을 따라 움직입니다.
5. **체험 종료 시 (`D` 또는 `Space` 키)**: 다음 관람객으로 교체하기 전 **`Space`** 또는 **`D`** 키를 눌러 `DISARMED (대기)` 상태로 전환합니다.

#### 💡 텔레옵 주요 단축키
* **`A`**: 로봇 추종 시작 (Arm)
* **`D` / `Space`**: 로봇 추종 안전 정지 (Disarm)
* **`C`**: 편 손 캘리브레이션 캡처
* **`F`**: 주먹 캘리브레이션 캡처
* **`R`**: 손 보정값 초기화
* **`H`**: 메인 홈 화면으로 복귀

---

### ✂️ Mode 2. 가위바위보 대결 (RPS Battle)

로봇과 관람객이 3-2-1 카운트다운에 맞춰 가위바위보를 겨루는 인터랙티브 게임 모드입니다.

#### 🎯 게임 진행 순서
1. **`Space`** 키를 누르면 화면 중앙에 **3 ➡️ 2 ➡️ 1** 대형 카운트다운이 시작됩니다.
2. 관람객이 카운트다운 끝나는 순간 손을 내밀면, 웹캠 비전 모델이 손 모양을 즉시 인식하여 로봇의 무작위 패와 비교해 승/무/패를 자동 판정합니다.
3. 화면 하단에 관람객 승/패/무 전적과 최근 대결 기록이 실시간으로 누적됩니다.
4. **연속 자동 대결 (`P` 키)**: 줄을 서서 여러 명이 연속으로 플레이할 때는 **`P`** 키를 눌러 `Auto-Play: ON`으로 설정하면 손을 인식할 때마다 자동으로 다음 라운드가 시작됩니다.

#### 💡 가위바위보 주요 단축키
* **`Space`**: 1회 대결 시작
* **`P`**: 자동 연속 대결 토글 (ON / OFF)
* **`R`**: 승패 스코어보드 초기화 (0승 0패)
* **`H`**: 메인 홈 화면으로 복귀

---

### 🎭 Mode 3. 제스처 쇼케이스 & 애니메이션

버튼 클릭 한 번으로 로봇 손의 정밀 제어 능력과 다양한 제스처, 역동적인 모션 애니메이션을 보여주는 시연 모드입니다. (화면 우측에 **내장 3D 시뮬레이터**가 함께 표시됩니다.)

#### 💡 쇼케이스 단축키 표

| 키 | 제스처 이름 | 설명 |
|:---:|:---|:---|
| **`1`** | ✊ **바위 (Rock)** | 모든 손가락 닫기 |
| **`2`** | 🖐️ **보 (Paper)** | 모든 손가락 펼치기 |
| **`3`** | ✌️ **가위 (Scissors)** | 검지/중지 펼치기 |
| **`4`** | 👐 **편 손 (Open)** | 기본 수평 정렬 자세 |
| **`5`** | 🖕 **중지 (Middle Finger)** | 중지만 신전 |
| **`6`** | 👍 **엄지 척 (Thumbs Up)** | 최고/좋아요 포즈 |
| **`7`** | 👌 **OK 사인 (OK Sign)** | 엄지-검지 핀치 결합 |
| **`8`** | ☝️ **가리키기 (Pointing)** | 검지만 뻗기 |
| **`9`** | 🤘 **락앤롤 (Rock On)** | 검지/소지 신전 |
| **`W`** | 🌊 **핑거 웨이브 (Finger Wave)** | 소지부터 검지까지 순차적 파도타기 댄스 |
| **`V`** | 👋 **손 인사 (Wave Hello)** | 손가락을 펴고 좌우로 까딱까딱 인사 |
| **`H`** | 🏠 **홈 복귀** | 메인 홈 화면으로 복귀 |

---

## 7. 🚨 비상 상황 조치 및 안전 수칙

### 1) 즉시 비상 정지 (Emergency Stop)
* **소프트웨어 비상 정지**: 어떤 화면에서든 키보드 **`Space`** 키를 누르면 즉시 토크 출력이 차단되고 대기 상태로 전환됩니다.
* **프로그램 강제 종료**: **`Q`** 키 또는 **`ESC`** 키를 누르면 모터 토크가 안전하게 꺼지며 시리얼 포트가 닫힙니다.
* **물리적 긴급 정지**: 로봇이 물리적으로 끼이거나 이상 동작을 보일 경우 **5V 전원 케이블을 즉시 뽑습니다**.

### 2) 관람객 안전 수칙
* 로봇이 작동 중일 때는 관람객이 로봇 손가락 사이에 손이나 물건을 넣지 않도록 안내합니다.
* 영유아 관람객의 경우 안전 거리를 유지하도록 지도합니다.

---

## 8. 🛠️ 현장 트러블슈팅 (FAQ)

### 오류 발생 시 즉시 대처 절차

#### 1) 손가락 각도가 이상하거나 의도하지 않은 방향으로 움직일 때

1. 터미널에서 **`Ctrl+C`를 한 번 눌러 현재 프로세스를 즉시 종료**합니다.
2. 터미널에 `[BOOTH] Kiosk shutdown complete.`가 출력되어 토크와 시리얼 포트가 안전하게 종료됐는지 확인합니다.
3. 로봇 손가락 주변에 사람의 손이나 물체가 없는 상태에서 부스 앱을 다시 실행합니다.

```bash
python booth_app.py --mode hardware --port /dev/ttyUSB0
```

> ⚠️ `Ctrl+C`를 눌러도 손가락이 계속 움직이거나 종료되지 않으면 즉시 5V 전원을 차단합니다. 재시작 후에도 같은 이상 동작이 반복되면 다시 ARM하지 말고 하드웨어 연결과 캘리브레이션 상태를 점검합니다.

#### 2) 하드웨어가 작동하지 않거나 일부 손가락이 응답하지 않을 때

1. 실행 중인 부스 앱을 `Ctrl+C`로 종료합니다.
2. 다음 명령으로 16개 모터의 연결 및 오류 상태를 확인합니다.

```bash
python leap_hand_hardware_check.py --port /dev/ttyUSB0
```

3. 출력에서 아래 두 항목을 확인합니다.
   * `Motor IDs/model numbers`에 모터 ID **0~15가 모두 표시**되어야 합니다.
   * `Hardware errors`의 **16개 값이 모두 `0`**이어야 합니다.
4. 모터 ID가 빠져 있거나 값이 `0`이 아니면 모터 또는 보드의 연결 부위가 헐거워져 케이블이 빠졌을 수 있습니다. **반드시 5V 전원을 먼저 끈 뒤** 커넥터와 배선을 다시 단단히 체결하고 점검 명령을 재실행합니다.
5. 16개 모터가 모두 인식되고 오류 값이 전부 `0`인 것을 확인한 뒤 부스 앱을 다시 실행합니다.

---

### Q1. "부스 앱을 실행했는데 로봇이 전혀 움직이지 않아요."
* **조치 1**: 5V 전원 어댑터 연결 상태와 USB 시리얼 케이블이 제대로 꽂혀 있는지 확인합니다.
* **조치 2**: 터미널에서 권한 설정 명령어를 다시 실행합니다:
  ```bash
  sudo chmod 666 /dev/ttyUSB0
  ```
* **조치 3**: 다른 프로그램(예: DYNAMIXEL Wizard 등)이 포트를 점유하고 있지 않은지 확인하고 완전히 종료합니다.

---

### Q2. "모터에서 삐- 소리가 나거나 `write address 102` 에러가 나면서 멈춰요."
* **원인**: 이전 실행이 비정상 종료되면서 다이나믹셀 버스 워치독(Watchdog) 안전 락이 걸린 상태입니다.
* **해결법**: 하드웨어 점검 스크립트를 1회 실행하면 워치독 락이 즉시 자동 해제됩니다:
  ```bash
  python leap_hand_hardware_check.py --port /dev/ttyUSB0
  ```

---

### Q3. "웹캠에서 사람 손을 잘 인식하지 못하거나 덜덜 떨려요."
* **조치 1 (조명)**: 부스 조명이 너무 어둡거나 강한 역광이 들지 않도록 카메라 각도를 조절합니다.
* **조치 2 (배경)**: 카메라 배경에 다른 사람들의 손이나 피부색과 유사한 복잡한 물체가 없도록 정돈합니다.
* **조치 3 (거리)**: 카메라와 관람객 손 사이 거리를 **0.6m ~ 1.0m** 정도로 유지하도록 안내합니다.
* **조치 4 (재보정)**: 텔레옵 화면에서 **`R`**을 눌러 보정을 초기화한 후, **`C`**(편 손) ➡️ **`F`**(주먹)를 다시 캡처합니다.

---

### 📞 비상 연락 및 기술 지원
* LEAP Hand 코드베이스 메인테이너 / 시스템 관리자에게 문의하세요.
