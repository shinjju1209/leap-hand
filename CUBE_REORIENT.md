# 인핸드 큐브 재배향 (Cube Reorient) 모드

부스 키오스크(`booth_app.py`)의 네 번째 화면. 학습된 강화학습 정책이 손 안에서
큐브를 목표 자세로 굴립니다.

**시뮬레이션 전용입니다.** 이 화면은 실물 손에 어떤 명령도 보내지 않습니다.
이유는 [§4](#4-왜-시뮬레이션-전용인가)에 측정값과 함께 적어뒀습니다.

---

## 1. 무엇을 보여주는가

화면 왼쪽에 **목표 큐브**, 오른쪽에 **손이 쥐고 있는 큐브**가 나옵니다.
관람객이 버튼으로 목표를 정하면 정책이 손가락을 움직여 큐브를 그 자세로 맞춥니다.
가운데 아래에 두 자세 사이의 **각도 오차**가 실시간으로 뜹니다.

- 오차 5.7° 이하 → 초록, `SOLVED` 표시 (학습이 성공으로 치는 기준인 0.1 rad)
- 45° 미만 → 주황
- 그 이상 → 빨강

3D 화면은 **오프스크린으로 렌더해서 키오스크 캔버스에 합성**합니다.
별도 MuJoCo 창이 뜨지 않으므로, 디지털 트윈을 끈 `--mode hardware`에서도
그대로 동작합니다.

---

## 2. 실행

```bash
# 부스 전체 (재배향 화면 포함)
python booth_app.py --mode mujoco

# 실물 손을 붙인 부스 운영 — 재배향 화면도 그대로 동작합니다
python booth_app.py --mode hardware --port /dev/ttyUSB0

# 재배향 화면만 끄기
python booth_app.py --no-reorient

# 장착 각도가 모델과 다를 때 어떻게 되는지 (중력을 기울입니다)
python booth_app.py --reorient-tilt-deg 20
```

홈 화면에서 `4`를 누르거나 네 번째 카드를 클릭하면 들어갑니다.

### 옵션

| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--reorient-policy` | `models/cube_reorient_policy.npz` | 학습된 정책 파일 |
| `--playground-root` | 설치된 패키지, 없으면 `~/Projects/mujoco_playground` | 장면 XML과 메시를 읽을 곳 |
| `--reorient-tilt-deg` | `0` | 시뮬 중력을 기울여 다른 장착 각도를 흉내 |
| `--no-reorient` | — | 이 화면을 끔 |

---

## 3. 조작

버튼과 키가 **같은 동작으로 연결**되어 있습니다. 버튼에 적힌 단축키가 곧 그 키입니다.

| 버튼 | 키 | 하는 일 |
|---|---|---|
| `X+` `Y+` `Z+` | `1` `2` `3` | 목표를 해당 축으로 +90° 회전 |
| `X-` `Y-` `Z-` | `4` `5` `6` | −90° 회전 |
| Random Goal | `R` | 무작위 목표 자세 |
| Reset Goal (Upright) | `0` | 목표를 기준 자세로 |
| Goal = Cube Now | `G` | 지금 큐브 자세를 목표로 (오차 0에서 시작) |
| Auto Goal | `A` | 학습에 쓰인 목표 스케줄 ON/OFF |
| Pause Policy | `Space` | 정책 일시정지 (자세는 유지) |
| Reset Cube & Hand | `X` | 큐브와 손을 기준 자세로 되돌리고 통계 초기화 |
| Return to Main Menu | `H` | 홈으로 |

> `Q`/`ESC`는 부스 전체 종료라 이 화면에서도 그대로 종료입니다.
> 그래서 −90° 축 키를 `Q/W/E`가 아니라 `4/5/6`으로 뒀습니다.

**Auto Goal**은 학습 때 쓰던 목표 스케줄입니다. 성공할 때마다 목표가 새로 튀고
큐브 쪽으로 서서히 되돌아옵니다. 버튼으로 세운 고정 목표는 그 스케줄의 극한에
해당합니다. 관람객 시연에는 고정 목표가, 정책 성능을 보려면 Auto Goal이 낫습니다.

---

## 4. 왜 시뮬레이션 전용인가

정책은 **실물 손이 아니라 학습 모델**을 상대로 만들어졌습니다. 두 모델을 실제로
불러 비교한 값입니다.

| | Playground (정책 학습 장면) | `scene_right.xml` (디지털 트윈) |
|---|---|---|
| 관절 이름·순서 | \_ | **동일** |
| 액추에이터 수 | 16 | 16 |
| **장착 각도** | **19.9° 기울어짐** | **0° (수평)** |
| **액추에이터 kp** | **3.0** | **12.0** (4배) |
| **관절 댐핑** | **0.2** | **0.03** |
| 타임스텝 | 0.01 | 0.002 |
| 큐브·목표·팔 센서 | 있음 | 없음 |

세 가지가 걸립니다.

**장착 각도 19.9°.** 학습 시 도메인 무작위화는 큐브 질량(×0.8\~1.2), 손끝 마찰
(0.5\~1.0), kp(×0.8\~1.2), 댐핑, 링크 질량, 관절 마찰을 전부 흔들었지만
**중력 방향만은 흔들지 않았습니다.** 정책이 유일하게 강인하지 못한 항목에서
20° 어긋나 있습니다.

**게인이 4배 다름.** 학습이 본 kp 범위는 ±20%뿐입니다.

**힘 한계.** 학습 모델의 액추에이터에는 `forcerange`가 걸려 있지 않습니다.
반면 실물 XL330-M288은 250 mA에서 대략 0.087 N·m 정도가 한계입니다.
정책은 실물이 낼 수 없는 토크를 전제로 움직입니다.

그래서 이 화면의 출력은 하드웨어로 가지 않습니다. 구체적으로는

- `step_smooth_control()` — 부스에서 모터로 가는 **유일한 경로** — 가 이 화면에서
  즉시 반환합니다.
- 화면에 들어가는 순간 `disarm_robot()`을 호출합니다. `4`로 armed 상태의 텔레옵에서
  곧장 넘어올 수 있는데, 그러면 아무도 안 보는 손이 토크를 문 채 남기 때문입니다.
- `cube_reorient_controller.py`가 하드웨어 관련 이름을 언급조차 하지 않는지
  테스트가 검사합니다.

가드를 지우면 `tests/test_cube_reorient.py`가 실패합니다.

---

## 5. 준비물

둘 중 하나라도 없으면 **이 화면만 비활성화**되고 나머지 세 모드는 정상 동작합니다
(콘솔에 이유가 출력됩니다).

1. **정책 파일** — `models/cube_reorient_policy.npz`
2. **MuJoCo 장면** — `assets/reorient_scene/` 에 함께 커밋되어 있습니다 (8 MB).
   따로 받을 것이 없고, 없을 때만 MuJoCo Playground 체크아웃을 찾습니다.

`mujoco_playground`를 **임포트하지는 않습니다.** 장면 XML을 문자열로 읽고 에셋을
파일 이름 기준으로 직접 모아 넘기기 때문에, jax·ml_collections 없이도 학습과
동일한 모델이 만들어집니다. (설치된 패키지가 있으면 그 위치를, 없으면
`~/Projects/mujoco_playground`를 봅니다.)

---

## 6. 구조

부스가 화면과 버튼을 갖고, 컨트롤러가 물리를 갖습니다.
`MujocoHandController`가 디지털 트윈에 대해 하는 역할과 같습니다.

```
booth_app.py
  AppScreen.REORIENT            화면 등록
  _init_buttons()               버튼 13개
  handle_reorient_action()      버튼·키 → 동작
  _render_reorient_screen()     3D 합성 + 오차 패널 + 텔레메트리
  step_smooth_control()         ← 이 화면에서 즉시 반환 (하드웨어 격리)

cube_reorient_controller.py
  CubeReorientController        정책 실행 · 목표 관리 · render_bgr()
                                step_if_due() 로 20 Hz 페이싱

cube_reorient/                  시뮬 코어 (leap_cube_pose에서 가져옴)
  policy.py                     npz 정책 (numpy만)
  observation.py                57차원 관측 구성
  goal.py                       목표 스케줄 · 자세 오차
  quat.py  mapping.py           쿼터니언 · 관절 매핑
  sim_backend.py                MuJoCo 물리 (에셋을 직접 읽음)
```

`cube_reorient/`는 `leap_cube_pose` 프로젝트에서 가져온 것입니다. 거기서 개발·테스트되며,
부스 장비가 이 체크아웃만으로 돌아가도록 임포트가 아니라 복사해 뒀습니다.
`sim_backend.py`만 두 군데가 다릅니다 — 에셋을 디스크에서 직접 읽는 것, 렌더용으로
`model`/`data`를 노출하는 것.

### 제어 주기

정책은 학습된 **20 Hz**로 돕니다. 키오스크는 화면이 허용하는 만큼 빠르게 다시 그리므로,
`step_if_due()`가 자체 시계로 페이싱합니다. 키오스크 전체를 늦추지 않습니다.
오프스크린 렌더는 프레임당 약 0.5 ms로 20 Hz 예산(50 ms)에 비하면 무시할 수준입니다.

---

## 7. 테스트

```bash
python -m unittest tests.test_cube_reorient -v
```

17개. 정책이 큐브를 떨어뜨리지 않는지, 목표 회전이 정확히 90°인지, 일시정지가
자세를 유지하는지, 20 Hz 페이싱이 지켜지는지, 그리고 **이 화면이 하드웨어에 닿지
않는지**를 확인합니다.

마지막 것은 반대 경우도 함께 검사합니다 — 쇼케이스 화면에서는 같은 호출이 실제로
손에 명령을 보내야 합니다. 가드를 그냥 지우면 테스트가 실패합니다.

전체 스위트:

```bash
python -m unittest discover -s tests
```

---

## 8. 알아둘 것

**실물 손에서는 아직 재배향이 되지 않습니다.** 이 화면은 정책이 무엇을 할 수 있는지
보여주기 위한 것이고, 실기 배포는 별개로 진행 중입니다. 현재까지 확인된 주요 간극:

- 장착 각도가 학습 모델과 약 20° 다름 (위 표)
- 카메라 기반 자세 추정이 대부분의 프레임에서 큐브 면을 두 개만 보며, 그 경우
  자세 점프가 발생
- 실물 큐브의 질량·마찰을 학습 범위(86\~130 g, 마찰 0.5\~1.0)와 대조한 적 없음

**부스에서 이미 있는 문제 두 가지를 이 작업 중에 함께 고쳤습니다.** 새 화면이 그대로
물려받는 문제였습니다.

- 홈 카드의 설명 텍스트가 카드 버튼에 덮여 보이지 않았습니다. 카드가 곧 버튼이고
  버튼이 사각형을 가득 채우기 때문입니다. 이제 버튼 위에 그립니다.
- 전역 내비게이션 키가 화면 키보다 먼저 처리되어, 쇼케이스 화면의 `[1] [2] [3]`
  버튼이 포즈 대신 화면 이동을 했습니다. 이제 화면 키가 우선합니다.

**남아 있는 것** (이번 범위 밖): 홈 화면의 `[Q] Exit` 버튼은 `handle_action`에 분기가
없어 클릭해도 반응하지 않습니다. 키보드 `Q`는 정상 동작합니다.
